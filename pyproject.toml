[project]
name = "comfyui-h3-prompt-ide"
version = "0.4.0"
description = "Standalone rich H3 prompt editor and reference-input palette for ComfyUI"
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
